import React from 'react';
import Layout from './Layout';

const Task = () => {
    return (
        <div>
            <Layout isOwner={true}>Task</Layout>
        </div>
    );
};

export default Task;

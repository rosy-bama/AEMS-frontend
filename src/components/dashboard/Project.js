import React from 'react';
import Layout from './Layout';

const Project = () => {
    return (
        <div>
            <Layout isOwner={true}>Project</Layout>
        </div>
    );
};

export default Project;

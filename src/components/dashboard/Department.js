import React from 'react';
import Layout from './Layout';

const Department = () => {
    return (
        <div>
            <Layout isOwner={true}>Department</Layout>
        </div>
    );
};

export default Department;

import React from 'react';
import Layout from './Layout';

const Leave = () => {
    return (
        <div>
            <Layout isOwner={true}>Leave</Layout>
        </div>
    );
};

export default Leave;

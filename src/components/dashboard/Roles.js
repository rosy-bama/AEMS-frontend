import React from 'react';
import Layout from './Layout';

const Roles = () => {
    return (
        <div>
            <Layout isOwner={true}>roles</Layout>
        </div>
    );
};

export default Roles;
